spring:
  datasource:
    # Puerto exclusivo del PostgreSQL Docker de este proyecto.
    url: jdbc:postgresql://localhost:5434/puce_market
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.springframework.security: INFO

app:
  user-contact:
    stub:
      phone-by-username: {}
